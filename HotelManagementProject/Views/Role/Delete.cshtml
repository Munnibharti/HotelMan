@model HotelManagementProject.Models.Roles

@{
    ViewData["Title"] = "Delete Roles";
}

<h1 class="text-center text-danger">Delete Roles </h1>

<div class="card mx-auto" style="max-width: 600px; border: 1px solid #ddd; padding: 20px; box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);">
    <div class="card-body">
        <h4 class="card-title text-center text-danger">Are you sure you want to delete this?</h4>
        <hr />
        <h5>Role Details</h5>
        <dl class="row">
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.Id)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.Id)
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.userName)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.userName)
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.Password)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.Password)
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.RoleType)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.RoleType)
            </dd>
           
        </dl>

        <form asp-action="Delete" method="post" onsubmit="return confirm('Are you sure you want to delete this Staff?');">
            @Html.HiddenFor(model => model.Id)
            <div class="form-group text-center">
                <input type="submit" value="Delete" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary ml-2">Cancel</a>
            </div>
        </form>
    </div>
</div>

<div class="text-center mt-3">
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>

